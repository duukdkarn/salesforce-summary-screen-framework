import { LightningElement } from 'lwc';
// CSS-only component — no logic needed.
// All theme tokens are defined in summaryScreenTheme.css.
export default class SummaryScreenTheme extends LightningElement {}